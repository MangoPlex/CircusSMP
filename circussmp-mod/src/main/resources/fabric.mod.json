{
  "schemaVersion": 1,
  "id": "circus-smp",
  "version": "${version}",
  "name": "Circus-SMP",
  "environment": "*",
  "entrypoints": {
    "server": [
      "top.circussmp.mod.SMPMod"
    ]
  },
  "accessWidener": "smp.accesswidener",
  "mixins": [
    "smp.mixins.json"
  ],
  "depends": {
    "minecraft": "${minecraftVersion}",
    "fabricloader": "${loaderVersion}",
    "fabric": "*"
  }
}